# 2026-08-24

## 小程序邀约详情「客户信息/跟进点」组件改造（已改代码）
- 改造 miniprogram/components/visit-note-section/（js/wxml/wxss），对齐 PC 端已定方向：每人一条 + 我填写的/同事填写的纯文字分区
- JS：loadNotes 按 created_by(_id) 归并取最新，can_edit 的那条=myNote；去掉 onEdit/onDelete 单条操作，onSubmit 有 myNote 走 update 否则 create；新增 onClearMine（清空=delete）
- WXML：卡片头 title+"N人已填写"；"我填写的"区（有=内容框+时间/清空，无=占位"写下你观察到的…"），整框 bindtap 开底部编辑弹层；"同事填写的"只读平铺无按钮；去掉右上角"新增"按钮；编辑弹层标题"我填写的{title}"、按钮"保存"
- 预览页：miniprogram/previews/visit-note-mobile.html（已填/未填两态 + 编辑弹层）
- 待办：后端 visit_notes "每人一条"唯一约束（visit_id, category, created_by_id）尚未实施，目前靠前端归并兜底
- 用户反馈字号偏大占空间：组件字号整体下调（标题 28→26rpx、内容 28→26rpx 行高 1.65→1.55、标签 24→22rpx、辅助 22→20rpx），卡片内边距与条目间距同步收紧（对齐 visit-card 的 21-24rpx 基线）；预览页同步更新
