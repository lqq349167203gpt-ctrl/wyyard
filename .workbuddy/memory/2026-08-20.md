# 2026-08-20

## 邀约/课表页只读态样式优化（进行中）
- 背景：刚上线"仅创建人可编辑/删除"权限，PC 端只读行用灰输入框+左灰条，用户嫌丑；且邀约表 17 列 min-w 1474px、课表 16 列 min-w 1315/1471px，需横向滚动
- 调研结论：只读逻辑在 frontend/src/components/visits/batch-input-table.tsx (canEditRow L129) 和 pages/daily-activities/activity-batch-table.tsx (canEditRow L319)，两页复制式重复实现，无共享组件
- 已出三组样式方案 mockup（A 纯文本化+锁签 / B 淡底分组 / C 分段视图"全部·我录入的"）+ 列收敛布局方案（行内展开收低频列），待用户选定方向后实施
- 用户倾向方案 C，要走了 C 的文案/改动规格去别处实施
