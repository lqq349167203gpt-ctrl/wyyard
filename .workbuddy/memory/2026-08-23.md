# 2026-08-23

## 邀约页多人录入（客户信息/跟进点）展示优化方案（仅出方案，未改代码）
- 现状：frontend/src/components/visits/visit-note-cell.tsx — 单元格 203px 只显最新一条"作者：内容"+N条；弹窗 440px 居中，记录按时间平铺，无归属区分
- 出了三套方案 mockup：
  1. 双行摘要单元格 + 按录入人分组弹窗（自己的带蓝标"我"）
  2. 作者圆点堆叠单元格（保持 28px 单行）+ 右侧评论串抽屉（仿飞书评论，我右蓝/人左灰）
  3. 悬停浮层预览 + 行内展开便签区（无弹窗，回车即存 Esc 收起）
- 外层单元格与内层面板可跨方案混搭；待用户选定
- **用户拍板方案三（悬停+行内展开），并纠正数据模型：不是评论串——每个使用者在客户信息/跟进点各只能有一条自己录入的内容，只能改自己的**
- 修正版展开区：按人排列每人一行；"我"固定第一位=可直接编辑的输入框（失焦自动保存/可清空），他人只读；未填写时我的一栏是空输入框；单元格计数"N条"改"N人"
- 注意：当前后端 visitNoteApi 允许同人同 category 多条 create，实施"每人一条"需后端加 (visit_id, category, created_by_id) 唯一约束或前端 upsert
- **用户否决行内展开（"很乱不舒服"），改为点击开居中弹窗**
- **弹窗再简化（用户反馈头像圆点、"我/还没填写"标签、虚线蓝框都看不懂）**：去掉一切头像/特殊边框/状态标签，用纯文字分区——"我填写的"（普通输入框，有内容失焦自动保存+清空，空则占位提示回车保存）+ "同事填写的"（名字+时间+内容平铺只读）。教训：给用户做 UI 避免暗示性视觉符号，直接写字最清楚
- 已交付：样式网页 docs/visit-note-redesign.html（单元格三态+悬停浮层+弹窗两态+行为规则）+ 完整实施提示词
- **客户详情页跟进点 tab 也需同步调整**：详情页读的是 visit 旧单条字段（feedback/healing_notes，见 customer_detail.py L64 + detail-view.tsx L507-525），与邀约页 visit_notes 多人数据是两套——新录内容详情页看不到。方案：卡片内两栏按人平铺（查看场景不开弹窗，"去编辑"跳回邀约页），后端 customer_detail 聚合接口附按人归并的 visit_notes，旧字段迁移后废弃。已补进样式网页第五、六节
- 关联：之前已出只读行三方案（用户倾向 C 分段视图）与邀约整表三布局（行内展开/双行记录流/主从双栏），均待拍板

- 邀约页工具栏单行合并方案：docs/visit-toolbar-compact.html（现状两行 vs 合并一行 40px + 窄屏兜底）；要点：到场人数与保存状态压缩、长提示缩为"仅创建人可编辑 ⓘ"悬浮、右侧操作成组（展开/导出/历史/撤回/重做）

- 课表页周主题日历压缩方案：docs/schedule-theme-compact.html（现状每周两行约226px → 方案一日期+主题并一行省约43%[推荐] / 方案二单周视图省约73% / 方案三摘要栏省约84%），待用户选定