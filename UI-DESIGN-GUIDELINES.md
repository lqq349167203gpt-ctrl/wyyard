# UI Design Guidelines — 无忧茶苑

## 设计原则
- 克制优先：少即是多，不要加不必要的装饰
- 只用两种字重：400（常规）和 500（强调），不用 600/700
- 用字号和颜色区分层次，不用字重堆砌
- 不要暗色主题，只做浅色
- 圆角要小（2-4px），不要大圆角
- 右边距要紧凑，不要留太多空白

## 排版层次（只用这 5 级）

| 层级 | 用途 | 字号 | 字重 | 颜色 |
|------|------|------|------|------|
| L1 标题 | 页面标题 | 18-20px | 500 | `#111` 或 `#1f2329` |
| L2 表头 | 表格列标题 | 11-12px | 450 | `#999` 或 `#8f959e` |
| L3 主数据 | 用户名、金额等关键信息 | 13px | 500 | `#111` |
| L4 次数据 | 类型、名称等 | 13px | 400 | `#333` / `#666` |
| L5 辅助 | 日期、成交人、备注 | 12px | 400 | `#bbb` |

## 空值占位
- 无数据用 `#ddd` 或 `#d0d3d6`，不用 `#8f959e`（太重）

## 数字对齐
- 数字列用 `tabular-nums` 等宽
- 金额右对齐，加 `¥` 前缀
- 金额用 `font-[500]` 加粗

## 颜色系统（现有）
- 主色：`#3370ff`（蓝色，用于链接、选中态、状态标签）
- 背景：`#f7f8fa`（页面背景）
- 卡片：`#ffffff`
- 文字主色：`#1f2329` / `#2b2f36`
- 文字次色：`#4e535a` / `#646a73`
- 文字辅助：`#8f959e`
- 分隔线：`#e8e8e8` / `#f0f0f0`
- 错误：`#f54a45` / `#c4506a`
- 成功：`#34c724`

## 表格设计规范
- 行高 48-52px
- 表头背景 `#f7f8fa` 或无背景
- 行间分隔线 `0.5px solid #f0f0f0`
- 悬浮高亮 `#f7f8fa`
- 操作列悬浮时才显示按钮

## Tab 切换规范
- 选中态：蓝色文字 `#3370ff` + 底部蓝色下划线
- 未选中：`#2b2f36` 文字，hover 变 `#4e535a`

## 按钮规范
- 主按钮：`#3370ff` 背景 + 白色文字
- 次按钮：白色背景 + `#e0e0e0` 边框
- 文字按钮：无边框，`#3370ff` 文字

## 输入框规范
- 高度 32px
- 边框 `1px solid #dee0e3`
- 圆角 `4px`
- 聚焦态：边框变 `#3370ff`

## 不要做的事
- ❌ 不要用渐变色
- ❌ 不要用大阴影（`shadow-lg` 以上）
- ❌ 不要用 emoji 装饰
- ❌ 不要给表格行加彩色背景
- ❌ 不要用彩色头像色块（灰色圆形首字母即可）
- ❌ 不要用花哨的来源标签（纯文字 + 小圆点即可）
