# 无忧茶苑管理后台视觉基线

适用于 frontend/ 的默认视觉标准；用户设计稿或指定参考页面优先。组件与交互见 [wyyard-ui-design](.agents/skills/wyyard-ui-design/SKILL.md)。

## 视觉原则

浅色、克制、信息清晰。默认字重 400/500，避免大面积装饰、渐变、大阴影与不必要的彩色色块。输入/普通按钮默认 4px 圆角，已有基类 6px 可保留；胶囊分段控件按其用途处理。

## 字体与颜色

| 用途 | 默认字号/字重 | 颜色 |
|---|---|---|
| 页面标题 | 18px / 500 | #1f2329 |
| 表头 | 13px / 400 | #8f959e |
| 正文/数据 | 13px / 400，必要强调 500 | #2b2f36 |
| 辅助信息 | 12px / 400 | #8f959e |
| Tab/弹窗标题 | 14px / 400 | #2b2f36 |

字体复用全局设置，不额外引入字体。可读性优先，非机械限定只能出现这些字号。

- 主色 #3370ff，用于可点击操作与选中态；正文不滥用主色。
- 页面背景 #f7f8fa，内容背景 #fff，分隔线 #f0f0f0 或 #e8e8e8。
- 次正文 #4e535a / #646a73，空字段短横线 #d0d3d6。
- 输入边框 #dee0e3，聚焦 #3370ff；错误使用现有 destructive token。
- 不直接用浅辅助色承载关键信息；警告/状态不能只靠颜色表达。

## 布局与控件

- 表格默认行高 48–52px，表头背景 #f7f8fa；多行记录允许增高。短数字列紧凑，长内容列获得剩余空间。
- 数字 tabular-nums，金额右对齐 + ¥ + 千分位，不强制加粗。
- Tab 默认蓝字和下划线；筛选分段控件可使用独立选中态，不能把所有选择都变成 Tab。
- 主按钮蓝底白字，次按钮白底细边框，默认高 32px；输入框同高，placeholder 与正文清楚区分。
