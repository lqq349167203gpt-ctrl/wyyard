# 2026-08-07

## 每日报表 v2 预览（样式定稿方向）
- 用户选定客户 tab 用「左色条身份卡」（D 样式），提出 4 项修改并产出 previews/daily-report-v2.html：
  1. 首行加会员身份文字标签（会员/新人，此前只有色条无文字）
  2. 统计行「今日」改名「今日活动」
  3. 密度提升：卡片 padding 12→9px、字号降一档（姓名 15→14、统计 12→11），单卡约 130px→95px，一屏 5 人→7~8 人；日期条/tab 栏压扁并吸顶
  4. 活动/财务/销卡三个 tab 页面已按同密度补齐
- 4 tab 框架（客户/活动/财务/销卡 + 日期条）已确认无问题。等用户确认 v2 后即可开始写小程序代码（pages/daily-report/index + 我的页菜单入口）。
- 待确认点：数据加载策略（按 tab 懒加载 vs 后端 /api/daily-report 聚合接口）。

## PC 客户详情页「卡次统计」tab 样式优化（进行中）
- 页面：frontend/src/pages/healing-records/components/detail-view.tsx 的 purchase tab。问题：所有信息 inline 连成一串 11px 灰字，看不清。
- 教训：用户要求"先给样式再改代码"——我一开始直接改了代码被叫停，已精确回退（只回退自己那处编辑；注意 wyyard 是 monorepo、git 根在 wyyard/，且该文件有用户自己的未提交改动，不能 git checkout）。
- 产出 previews/purchase-stats-redesign.html 现状 vs 方案对比：汇总 17px 大字（剩余 N 次）+ 扣减构成 chips + 每卡一行四列对齐（卡名/次数/有效期/状态徽标）+ 购买明细降级为缩进子行。数据字段与分页逻辑不变，待用户确认后再改代码。

## 卡次统计 v2：统一页面语言（进行中）
- 用户否决上一版："七八种样式太花了，要清晰整洁，和页面其他内容统一"。
- 从 detail-view.tsx 其他 tab 提取的页面语言（重要，后续改动要遵守）：跟进点=浅灰卡(#fafbfc+border #eef0f1+rounded-10)+84px 标签栅格(label #a8b1bd/值 #3a4150 12px)+pill(10.5px semibold rounded-full，绿#157a3c/#dcf5e4、灰#79838f/#f1f0ed)；沟通记录=左 130px 日期+右内容 hairline 行；用户回访=hairline 行+灰底小签(#f1f3f5/#68717d)+12px 灰 meta 行+ #fafafa 内容块；文字色阶 #212631/#3a4150/#8f959e/#a8b1bd。
- 新产出 previews/purchase-stats-v2.html：会员卡合计用跟进点卡片语言（不做大数字），所有类型统一成 hairline 行（类型/名称/剩余粗体600/有效期/状态 pill），购买明细=12px 灰字子行。只 3 种字号 4 种颜色。待用户确认后改代码。

## 卡次统计 v3：表格版（进行中）
- v2 仍被否："乱糟糟"。根因诊断：每行信息长短不一、无列对齐、右侧参差。
- v3（previews/purchase-stats-v3.html）改为严格表格：名称/次数/有效期/状态四列固定宽度、行高一致、名称缩进从属于分组头；「会员卡」分组头右侧直接带汇总（不再单独汇总卡）；唯一强调=剩余次数 600 字重；状态纯文字（红/灰），无 pill/chip；购买明细默认收起、点行展开为一行灰字。对齐「交易记录」tab 的表格观感。待用户确认。

## 卡次统计 v4：平级类目版（进行中）
- 用户指出 v3 的结构错误：会员卡/觉醒游戏/情绪释放/OH卡梳理/能量结/内部课程/其他项目是 **7 个平级类目**，不能"会员卡特殊 + 其他合并"。
- v4（previews/purchase-stats-v4.html）：每个类目一个区块，模板统一 = 类目头（类目名 13px bold + 右侧该类合计"剩余 X / 共 Y 次"）+ 条目行四列固定（名称/剩余/有效期/状态）。会员卡的每张卡、游戏类的每次购买都是类目下的平级条目；扣减构成（扣卡/销卡/预支）降级为类目头下 12px 浅灰附注。空类目整块不显示。待用户确认。

## 卡次统计 v5：平级类目 + 页面同步样式（待确认）
- v4 结构用户认可（"在这个基础上"），要求样式与页面其他 tab 同步。
- v5（previews/purchase-stats-v5.html）：类目卡=跟进点浅灰卡(bg #fafbfc+border #eef0f1+rounded-10+mb-9px)；类目名=12.5 bold #212631；条目行=用户回访 hairline(#f0f1f2)；状态=pill 语言(生效中绿 #157a3c/#dcf5e4、无次数/未生效灰 #79838f/#f1f0ed、已过期红 #c4506a/#fdeeee)。文件底部附 tailwind 对照表，确认后可直接照此改 detail-view.tsx purchase tab。
