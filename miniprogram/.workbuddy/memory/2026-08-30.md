# 2026-08-30

## 重要更正：邀约/课表样式目标是 PC 端
- 8-20 用户要求看「邀约和课表页面」样式，我做了**小程序**版方案（`previews/visits-activities-styles.html`）——用户随后纠正：**要的是 PC 端（frontend/ React 后台）的**。小程序版方案作废，PC 端方案待重做（用户暂时转而提了登录页任务）。

## 员工端小程序登录页样式（09:55）
- 任务：优化 `pages/login` 登录页，出 2 个样式供选择（只出稿）。
- 现状问题：135° 线性渐变按钮（用户历来嫌 CSS 渐变廉价）、整页背景图 /images/3.jpg + 负边距(-100rpx) hack 定位、16rpx 大圆角、按钮带阴影。
- 产出 `previews/login-styles.html`：A 极简居中（白底+underline 输入框+小标签，类 Linear/飞书）/ B 色带分区（顶部淡蓝灰 #edf2fa 平色品牌带硬边分割 + 填充输入框）。两版共用：平色 #3370ff、4-6rpx 小圆角、无渐变无阴影无背景图，内容/逻辑不变。
- 等用户选定后落地 pages/login 的 wxml/wxss。

## 管理端客户页局部配色（20:58）
- 用户只希望调整 `pages/customers/index` 的顶部背景与客户标签色，其他现有颜色保持不变；后续局部视觉任务必须严格控制范围。
- 当前顶部为 `#DDEAF4`，标签为 `#F3F9FC / #79BEDF`；标签文字偏浅、显得飘。
- 提供三套同风格微调：A 清透蓝灰、B 极淡品牌蓝、C 中性浅灰。
- 用户要求查看完整页面效果，已生成只读预览 `outputs/customer-page-brand-blue-preview.html`；小程序源代码未修改。
- 第一版极淡品牌蓝仍被用户否决；用户提供粉紫蓝 UI 配色参考图。按参考图更新预览：顶部使用哑光蓝紫灰 `#D1D0EF`，标签使用 `#F0F0FA / #6669A6`，不采用参考图渐变或粉色，其余颜色沿用现状。
- 随后用户要求在真实小程序中试用系统主蓝，但查看后要求撤回；已将 `pages/customers/index.json` 标题栏和 `pages/customers/index.wxss` 搜索栏外层恢复为试色前的 `#F3F7FF`，导航栏文字恢复黑色。
- 客户列表标签减淡文字方案被否决；当前 `.c-tag` 最终试色为：底色单独减淡至 `#F5F7FF`，文字保持系统蓝 `#3370FF`，字重保持较细的 `400`。筛选标签未动。
- 将客户列表分组标题的黄色 `#F4DC84` 应用到客户详情页各信息区小标题前的 `.group-dot`；详情页记录 Tab 栏及其 `.dot-blue/.dot-gray/.dot-warn` 状态圆点完全未动。
- 客户详情页信息区内的 `.identity-tag` 与列表页 `.c-tag` 统一：底色 `#F5F7FF`、文字 `#3370FF`、字重 `400`；顶部 `.hero-tag` 和记录状态 `.pill` 未动。
- 邀约页视觉统一：`pages/visits/index.json` 标题栏明确设为与客户页一致的 `#F3F7FF`、黑色文字；页面 `.head` 原本已是 `#F3F7FF` 无需再改。邀约名单标题前 `.list-heading-dot` 改为黄色 `#F4DC84`；周历/日历的业务状态圆点未动。
- 按邀约页现有视觉语言制作课表页两版只读预览 `outputs/activities-visit-style-options.html`，未改真实课表代码。A 为连续课表（一张白卡、固定时间纵列、扫读最快）；B 为时段分组（上午/下午/晚上各一张白卡、边界更清晰）。两版均使用 `#F3F7FF` 连贯顶部、邀约同款周历、黄色 `#F4DC84` 标题圆点和系统蓝选中态。
- 邀约页头部进一步精简：删除日期下方“当日 N 位邀约 · N 已到店”整行；将“排序 / 导出”移动到“邀约名单 N 人”标题右侧并替换“按当前顺序”；去掉空间名称右侧下拉箭头但保留 picker 功能；同步将列表顶部占位从 `334rpx` 收紧到 `270rpx`，空态/加载态上边距同步减少。
- 修复课表两版预览周历下方异常大空白：原因是 `.head` 被误写死为 `258px`，而实际内容高度约 `169px`；已移除固定高度，并将 `.content` 高度扣除值从 `334px` 修正为 `245px`。
- 用户选定课表 B 方案并已落地真实页面：标题栏/头部改为 `#F3F7FF`，加入邀约同款周日期条；列表按上午（<12）、中午（12–14）、下午（14–18）、晚上（≥18）分组，每组标题前使用黄色 `#F4DC84` 圆点，并采用“一时段一卡、组内分行”。无“今日课表”总标题；退课按钮从头部移到第一个可见时段标题右侧，避免上午无课时功能消失；空间名称去掉下拉符号但保留 picker。脚本与 JSON 语法检查通过。

## 课表页再优化两版样式（21:50，仅出稿未改代码）
- 产出只读预览 `previews/schedule-page-v2.html`（375pt 手机框，1px = 2rpx，可直接换算落地）。
- A 紧凑清单：一个总标题「今日课表 5 场 · 28 人」+ 退课固定右侧，一张白卡连续排完，行高 104rpx，一屏 7–8 场；状态为灰色小签 `#f2f3f5`。
- B 时段分组：在已落地的 B 上打磨，段标题仍用黄色 `#F4DC84` 圆点 +「N 场」，段间留 16rpx，行高 120rpx，右列人数放大到 30rpx/500 作锚点，状态内联到课程名右侧的淡蓝小签（沿用客户页 `#F5F7FF / #3370FF`），一屏 5–6 场。
- 两版共用：删「今日 N 位参与者」整行、顶部占位 `334rpx → 292rpx`、空态/加载态 `470rpx → 420rpx`、创建人改「·」分隔、时间列 tabular-nums、无阴影无渐变、卡片维持 24rpx 圆角 + `#f1f2f4` hairline。
- 已知取舍：B 的退课挂在第一个可见时段标题右侧，位置会随上午有无课漂移；A 无此问题但时段边界弱。

## 课表页 B 结构落地（21:56，仅改结构，其他样式未动）
- 用户选定 B 后明确要求「只调整结构，其他不要动」，改动严格限制在以下两处：
- `pages/activities/index.js` 的 `groupByTimeSlot`：五段改三段 —— 上午 `<12:00`、下午 `12:00–18:00`（原「中午 12–14」并入下午）、晚上 `≥18:00`；无时间的记录仍落到「其他」并排在最后，空组依旧 `filter(s => s.items.length > 0)` 不渲染。
- `pages/activities/index.wxss` 的 `.group`：`margin-bottom: 12rpx → 16rpx`（6px → 8px 段间呼吸）。
- 段标题的「N 场」原本就有（`wxml` 的 `.group-count`），未改；头部、状态、人数、卡片等一律未动。JS 语法检查通过。
- 后续局部视觉任务继续沿用此原则：先确认改动清单再动手，不顺带优化用户没点到的元素。

## 课表页删除「今日 N 位参与者」行（21:58）
- `pages/activities/index.wxml`：删掉日期下方的 `.head-row2`（今日 {{participants.length}} 位参与者）整块。
- 头部变矮导致的必要联动（否则列表顶部空一块，与邀约页头部已对齐）：`.list` padding-top `334rpx → 270rpx`；空态 `.empty-state` 与 `.loading` 的上边距 `470rpx → 406rpx`。
- `.head-parti / .cnt / .head-row2` 的 wxss 类定义未删（无害、暂留）；`participants` 数据仍在 js 中收集，仅 wxml 不再展示。
- 课程行、卡片、状态、人数、段分组等其余样式一律未动。

## 课表页段间距对齐样例 B（22:00）
- 用户要求段（上午/下午/晚上）间距与样例 `schedule-page-v2.html` 的 B 版一致；样例 B 段标题块高约 34px、段间距（卡片到卡片）约 42px（8px 段距 + 34px 标题块）。
- `pages/activities/index.wxss` `.group-heading` 的 padding：`32rpx 40rpx 16rpx` → `20rpx 40rpx 20rpx`（上/下各 20rpx≈10px，使标题块高≈34px，段间距视觉对齐样例）；`.group margin-bottom` 维持上一步的 `16rpx`（=8px 段距）。
- 课程行之间的分割线经核对已与样例 B 同款（`1rpx solid #f1f2f4`），未改动。
- 小结：局部样式任务优先对照已确认的样例像素值落地，不顺手优化其他元素。

## 邀约页图标与箭头对齐课表页（22:0x）
- `pages/visits/index.wxml`：空间名左侧 `<view class="space-pin">` 蓝色定位钉图标删除；日期右侧下拉箭头由 CSS 绘制的 `<view class="drop-arrow">` 改为与课表页一致的 `<text class="drop">{{calendarExpanded ? '▴' : '▾'}}</text>` 文本三角。
- `pages/visits/index.wxss`：删 `.space-pin`/`.space-pin::after`（并去掉 `.space-content` 的 `gap:10rpx`，单元素 gap 无效）；删 `.drop-arrow`/`.drop-arrow.open`，新增 `.head-date .drop { margin-left:12rpx; color:#8f959e; font-size:22rpx; }`，逐字对齐 `pages/activities/index.wxss` 第 38–42 行。
- 空间胶囊底框高度对齐课表页：`.space-content` 由写死 `height:64rpx` 改为 `padding:14rpx 22rpx` 撑高，与 `pages/activities/index.wxss` 的 `.head-space` 完全一致（圆角 20rpx / 白底 / `#646a73` / 24rpx 均同）；周历、字体等其余未动。两页头部展开指示器外观与交互现已一致。

## 课表页去课程名彩点 + 显示方式对齐样例图 B（22:0x）
- 去彩点：`pages/activities/index.wxml` 课程行 `card-line1` 内 `<view class="dot" style="background-color:{{item.color}}">` 删除；`index.wxss` 删 `.dot` 定义，并把 `.name` 因圆点预留的 `margin-left:12rpx` 归零（名字不再缩进）。
- 显示方式对齐 `previews/schedule-page-v2.html` 的 B 方案：每时段「一时段一白卡」包多门课，相邻课 `1rpx solid #f1f2f4` 分隔。`.group-card` 恢复白底圆角（`overflow:hidden;margin:0 24rpx;border-radius:24rpx;background:#fff`），`.card` 恢复白底；相邻课行高/内边距对齐样例（`min-height:100rpx→120rpx`、`padding:22rpx 24rpx→20rpx 28rpx`，对应样例 60px/10px 14px，1px=2rpx）；`.card + .card` 的 hairline 与样例 `--line:#f1f2f4` 一致，未动。
- 注：曾按"双分割线"反馈临时改成"去白卡框、列表落灰底单 hairline"，用户随后推翻，要求整体显示方式回样例图 B（白卡一时段一卡），仅相邻课间距按样例图。彩点保持去掉（用户上次明确指令，本次只说间距、未要求恢复）。
- 课程行其余（时间列、状态小签、人数、段分组）未动。

## 课表页活动类型显示全称（22:13）
- 用户要求活动类型小签不显示简称：示例 gcs（觉醒游戏）不应显示"觉醒"、真实"游戏"类型也不应误显成"觉醒"。
- 根因：`pages/activities/index.js` 的 gcs 记录 `badge` 写死 `'觉醒'`（简称，源自 `activity-constants.js` 的 `TYPE_LABELS.gcs`）；课程行 `typeText = [badge, 公益].join(' · ')` 因此显示简称。
- 改动：`badge: '觉醒'` → `'觉醒游戏'`、`color: BADGE_COLORS['觉醒']` → `BADGE_COLORS['觉醒游戏']`（#8b72c7 本就存在）。现类型小签显示全称「觉醒游戏」。
- 「游戏不显示觉醒」天然成立：class（沙龙活动）分支 `badge = r.course_type || '沙龙'`，真实 course_type 为"游戏"时原样显示"游戏"，不受 gcs 改动影响。JS 语法检查通过，`index.js` 已无 `'觉醒'` 简称残留。
- 注：仅改课表页 gcs 分支，未动 `activity-constants.js` 的 `TYPE_LABELS`（属共享常量，其他页面可能仍在用简称，改动需评估影响）。

## 课表页去掉卡内 hairline 消除双分割线（22:15）
- 用户反馈恢复「白卡一时段一卡」后每堂课又出现两条分割线，要求去掉多余的一条并自行判断。
- 判断：结构为「灰底 `#f7f8fa` 上的白色 `.group-card` 卡片（用户 22:13 明确要求保留）」+ 卡内 `.card + .card { border-top: 1rpx solid #f1f2f4 }` 的 hairline；两者叠加使段内门课被夹成双线观感。白卡为用户点名保留，故多余的是**卡内 hairline**。
- 改动：`pages/activities/index.wxss` 删除 `.card + .card { border-top: ... }` 整条。现每时段为一张干净白卡，门课靠 `min-height:120rpx` 行高排列，双线消除。
- 取舍：段内门课之间不再有细分隔线（仅白卡块边界作时段分隔）。若后续用户希望门课之间保留单线，需二选一：要么恢复 hairline 并去掉白卡框（回到灰底单 hairline），要么白卡+hairline 接受双线观感——当前按"白卡优先"执行。

## 课表页最终去除白卡框、回到灰底单 hairline（22:21）
- 用户反馈"最后一段白卡底边还有一条多余分割线"——根因是白卡 `.group-card` 白块在灰底 `#f7f8fa` 上的边界（尤其最后段白卡下方是纯灰底，块边界最显眼），被当作多余线。
- 用户 22:13 要求"白卡一时段一卡（跟样例图）"在真机上边界观感不佳，现推翻：去掉 `.group-card` 的 `overflow/border-radius/background`，仅留 `margin:0 24rpx`；同时去掉 `.card` 的 `background-color:#fff`（否则每门课仍是白块）。
- 恢复门课之间单条 hairline：`.card + .card { border-top: 1rpx solid #f1f2f4 }`，作为唯一分隔线（无白卡边界叠加）。
- 最终态：列表落灰底 + 门课单 hairline + 段标题分组（上午/下午/晚上）；最后一段下方无线、无白卡边界。即回到 22:0x 最初认可的"去白卡框留单 hairline"方案（被 22:13 白卡需求打断，现回归）。
- 注：样例图 `schedule-page-v2.html` 的 B 版为白卡一时段一卡，与真机落地的"灰底单 hairline"已不一致；若后续要以样例图为准需重做白卡，否则以真机干净单线为准。

## 课表页纠正：同段课程必须打包在白卡内，不可拆开（22:24）
- 用户纠正 22:21 方案：不要拆开"相同区（上午/下午/晚上）内"的课程。22:21 去掉白卡框导致同段门课散落灰底被 hairline 分行，即"拆开"，用户不接受。
- 恢复到 22:15 状态（白卡一时段一卡、门课不拆开）：`.group-card` 恢复 `overflow:hidden; margin:0 24rpx; border-radius:24rpx; background:#fff`；`.card` 恢复 `background:#fff`；**保持删除 `.card + .card` 的 hairline**（门课之间无线，避免白卡边界+hairline 双线）。
- 关键澄清：用户 22:21 嫌的"白卡底边多余线"实为白卡 `.group-card` 在灰底上的卡片边界，这是"白卡一时段一卡"的必然产物——要打包门课就必须有卡片边界，二者互斥；用户此轮明确"不拆开门课"优先，故白卡边界保留（现为唯一边界线，非双线）。
- 当前终态：每时段一张白卡打包该段全部门课，卡内门课靠 `min-height:120rpx` 行高排列、无细分隔线；段与段靠段标题+16rpx 间距。即"白卡一时段一卡 + 门课不拆开 + 卡内无线"。
- 教训：该用户要的是"卡片分组打包"（层级靠卡片块），不是"灰底列表+行线"；之前 22:0x/22:21 两次"去白卡框"均误判，其真实诉求始终是白卡打包 + 仅去门课之间的细线。

## 课表页最终去白卡背景、消除底边多余线（22:27）
- 用户指出"同一时段内最后一张卡后面还有一根多余的细线"。根因确认：`.page` 背景 `#f7f8fa` 浅灰，白卡 `.group-card` 白底 `#fff`，最后卡后的"线"是白卡在灰底上的底边（白块 vs 灰底色界，非 1px 描边，是卡片边界本身）。
- 技术互斥：保留白卡打包（22:24 要求不拆开门课）就必然有这条底边；要消除它只能去掉白卡背景。用户 22:27 明确要去掉该线，按"去线优先"判断。
- 改动：`.group-card` 去掉 `overflow/border-radius/background-color`，仅留 `margin:0 24rpx`（左右内边距使同段门课聚拢、不散落满宽，即"不拆开"）；`.card` 去掉 `background-color`。门课之间 `.card + .card` hairline 此前已删，保持无线。
- 最终态：列表落浅灰底；同段（上午/下午/晚上）门课靠 24rpx 内边距聚拢 + 段标题分组，不拆开；无白卡底边线、无门课细线、无双线。
- 退路：若用户其实要"可见白卡块"（接受底边为卡片边），一键恢复——`.group-card` 加回 `overflow:hidden;border-radius:24rpx;background-color:#fff`，`.card` 加回 `background-color:#fff`。（注：此退路已由 22:32 触发执行）
- ⚠️ 此条（22:27）已被 22:32 推翻：用户明确"不拆开"优先，恢复白卡打包，白卡底边作为卡片边界保留。

## 课表页终态确认：白卡打包不拆开 + 门课无线（22:32）
- 用户原话"怎么又被你给拆开了"推翻 22:27 的"去白卡背景"方案：去白卡使同段门课散落灰底被当作拆开，不可接受。
- 恢复：`.group-card` 加回 `overflow:hidden; border-radius:24rpx; background-color:#fff`；`.card` 加回 `background-color:#fff`；门课之间 `.card + .card` hairline 保持删除（卡内无线，避免白卡边界+hairline 双线）。
- 终态（铁律）：每时段一张白卡打包该段全部门课、不拆开；卡内门课靠 `min-height:120rpx` 行高排列、无细分隔线；段与段靠段标题 + 16rpx 间距。
- 矛盾澄清：用户既要"同段门课打包成白卡（不拆开）"、又曾嫌"最后一张卡后面的线"（=白卡在灰底上的底边）。二者技术互斥——白卡打包必有卡片底边。本轮以"不拆开"优先，白卡底边保留（它是卡片块边界，非多余分割线）。
- 铁律：课表页同段门课必须留在白卡内，不得再去除 `.group-card`/`.card` 的 `background-color`（22:21、22:27 两次误判去白卡均被推翻）；门课之间一律无线（此条被 22:35 推翻）。

## 课表页门课之间加 hairline（中间线、最后底无线）（22:35）
- 用户明确：同段内课程"只需要中间的那根分割线，最后一门课程底部的分割线不要"。
- 根因复盘：此前 22:15 起反复横跳，是把"白卡 `.group-card` 在灰底 `#f7f8fa` 上的卡片底边"与"门课之间的 hairline"两件事混为一谈。用户说的"分割线"指门课之间的 hairline；白卡底边是打包边界（用户"不拆开"硬需求），二者并非同一根线，之前误把白卡底边当多余线反复去/留，导致来来回回。
- 改动：加回 `.card + .card { border-top: 1rpx solid #f1f2f4 }`。该选择器只给"非首张门课"加顶边线，故门课之间有中间分隔线、最后一张门课无 border-bottom（最后底无门课线）。白卡 `.group-card`（白底圆角）与 `.card` 白底均保留（打包不拆开）。
- 终态（确认）：每时段一张白卡打包全部门课；门课之间 1rpx hairline（中间分隔）；最后一张门课底部无门课线；白卡底边作为卡片打包边界保留。
- 铁律：① 同段门课必须留在白卡内（不得去 `.group-card`/`.card` 背景，22:21、22:27 两次去白卡均被推翻）；② 门课之间保留 `.card + .card` hairline（中间线），但不得给最后一张门课加 border-bottom；③ 不要把白卡底边当成"多余分割线"去消除（会破坏打包、用户会认为拆开）。
- 22:39 复核发现此前遗漏：课表 `.card` 会继承 `app.wxss` 全局 `.card { margin-bottom:16rpx; box-shadow:... }`，这会让最后一门课程底部看起来像多出一条线。已在 `pages/activities/index.wxss` 改为更高优先级的 `.group-card .card`，明确 `margin-bottom:0; box-shadow:none; border-radius:0`，并将相邻线收紧为 `.group-card .card + .card { border-top:1rpx solid #f1f2f4 }`，最后一门明确 `.group-card .card:last-child { border-bottom:none }`。终态：同段白卡不拆开；只显示课程之间的中间线；最后一门底部不显示课程线/阴影/间距线。

## 课表页公益读书会名称前标签（23:01）
- 用户要求：活动类型为“读书会”且标记为公益时，在课程名称前以标签形式显示“公益”。
- `pages/activities/index.js`：class_record 记录新增 `isBookClubWelfare: badge === '读书会' && !!r.is_public_welfare`；构建 `typeText` 时，公益读书会不再追加名称后的“公益”，其他公益活动保持原逻辑。
- `pages/activities/index.wxml`：在 `.card-line1` 中，名称前新增条件渲染 `<text class="welfare-tag">公益</text>`。
- `pages/activities/index.wxss`：新增 `.welfare-tag`，使用小号蓝色标签（浅蓝底、#3370ff 字色、6rpx 圆角），仅该条件记录显示；其余布局和业务逻辑不变。
- 校验：`index.js` 语法通过、`git diff --check` 通过，关键词引用已核对。
- 23:07 用户反馈蓝绿色青柠版不好看，要求走清亮方向并接近时段标题黄色圆点。`pages/activities/index.wxss` 的 `.welfare-tag` 已调整为清亮黄方案：底色 `#f4dc84`（与 `.group-heading-dot` 同色）、边框 `#e5c45e`、文字 `#62530e`；尺寸收紧为高度 `30rpx`、字号 `20rpx`、左右内边距 `8rpx`、圆角 `5rpx`、右间距 `8rpx`，视觉更轻。其他标签和布局未动，`git diff --check` 通过。
