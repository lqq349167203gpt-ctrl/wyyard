# 2026-09-01

- 付费项目页（pages/payment）样式优化两版预览 `outputs/payment-page-style-options.html`（只读，未改代码）。现状问题：9 个横滑标签+导出按钮挤一行、卡片灰色小字平铺无层级、FAB 用了 linear-gradient（违反"无渐变"偏好）、卡片带阴影。两版共同处理：状态做淡底小签（生效中 #EEF3FF/#3370FF、已过期 #FAEEF1/#C4506A、未开始灰）、价格从蓝色改墨色 600、FAB 去渐变改实色蓝+减淡阴影、卡片去阴影。A：标签横滑细线样式搬进浅蓝头部（对齐其他工作页）、备注独立成行加 hairline；B：白底页+九类型平铺 chip 标签（免横滑）、次卡类购买/剩余/有效期做成卡内统计列（剩余 0 红色警示）。等用户选择或混搭。
- 用户选定 A 方向（浅蓝头+细线标签），并要求卡片内显示购买/剩余次数。已把预览 A 更新为统计卡形态（demo 切到觉醒游戏 tab）：卡片 = 名字+状态小签+墨色价格 / 套餐 meta 行 / 购买·剩余·有效期三列统计（hairline 分隔，数字 600 墨色、标签小灰字，剩余 0 红）/ 备注行。无次数的类型自动隐藏统计列只留日期行。待用户确认后落地 pages/payment。
- 用户再提两点：卡片要显示创建人；且嫌三列大统计卡太占空间（一屏放不下几个）。已把 A 改成紧凑三行卡：行1 名字+状态小签+墨色价格；行2 meta（套餐 · 创建人）；行3 内联统计条（购买 N · 剩余 N · 至 日期，hairline 上边线，数字 600 墨色内联、剩余 0 红），卡内边距收紧（11/14px），每卡省约 1/3 高度；备注仅在存在时显示单行截断。

## 课表名单入口+参与人弹窗样式方案（11:30，仅出稿）
- 背景：课表卡新增「名单」入口（`participant-entry` = N人+蓝字名单+浅蓝› 三拼元素，堆在右列与仅查看/已发布挤三层）；点开后底部弹窗 `participant-sheet` 每行一个蓝边「查看资料」按钮（跳客户资料）。用户嫌入口位置不舒服、弹窗按钮位置也要调。
- 产出 `previews/schedule-roster-styles.html`（3 帧，页面其余 1:1 还原真机现状：白卡打包/黄色段标题点/周日期条/退课入口）：
  - A 人数胶囊：右列「6 人 ›」灰色胶囊（人数即按钮，与状态签同色）+ 弹窗整行可点（去全部按钮，右侧灰箭头+引导小字）
  - B 头像串：meta 行改头像串+等N人›，右列只剩状态签 + 弹窗 4 列头像网格（已退课置灰删除线），点头像跳资料
  - C 课内名单条：课程行内下方缩进印「6 人·陈默、璐小娜…」+名单› + 弹窗保留列表、按钮换纯文字「资料 ›」细链接
- 等用户选定（可混搭），落地只动 pages/activities 名单入口与弹窗两处。
