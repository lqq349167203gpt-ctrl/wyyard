<view class="page">
  <view class="loading" wx:if="{{loading}}">
    <text class="loading-text">加载中...</text>
  </view>
  <payment-form wx:if="{{!loading && editData}}" type="{{type}}" is-edit="{{true}}" edit-data="{{editData}}" bind:success="onFormSuccess" />
</view>
