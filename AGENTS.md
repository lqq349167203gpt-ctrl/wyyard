# wyyard — 项目协作约定

## 任务边界

- 系统、开发者指令与工具权限优先；用户当前要求优先于项目默认规则。Skills 只补充领域细节，不另设批准流程。
- 解释、审阅、诊断只读；“先给方案”只交付方案或所需预览；明确要求修改后，完成范围内实现、必要联动和验证，不额外做全站改版、重构或历史问题清理。
- 普通实现细节自主决定。仅在缺失信息会改变业务口径、权限、数据目标或产生难以恢复的后果时澄清，先做不依赖答案的安全工作；已有明确选择或授权不重复询问。
- 以当前源码和配置为准；历史聊天、记忆、预览和旧示例只是线索，不作为现行规则或授权。定位根因，复用已有实现；工具失败先查原因或安全替代，真实阻塞如实报告。
- 子代理仅在当前运行规则允许且确有独立价值时使用，不为普通小改动增加协作流程。

## 授权与数据安全

- 范围内源码修改、隔离测试和可恢复开发产物无需另问；推送、发布、全局依赖安装、真实数据删除/覆盖、破坏性迁移及密钥轮换须有对应动作与目标的明确授权。编写迁移不等于获准在真实数据库执行，工具批准仍须遵守。
- 测试先核实连接目标及隔离库/schema；localhost 不代表可删除，连接失败不转用正式库。数据源仅切换用户指定端与环境，目标不明先问。
- 业务记录必须持久化，临时 UI 状态、缓存和测试替身除外。密钥、token、密码不进入源码、提交或日志，使用现有环境配置。
- 保留已有改动；本次无引用临时代码可清理。既有疑似废弃文件先核对调用方，需要清理时归档至 archive/ 至少 30 天；兼容接口保留并标明替代方案，不批量清理。
- 禁止 git reset --hard、强制覆盖工作树或绕过沙箱。恢复/覆盖业务数据前核实准确目标并准备可恢复备份。

## 项目与实现约定

- frontend/：React + TypeScript + Vite；组件 src/components/，页面 src/pages/，请求 src/lib/api.ts。
- backend/：FastAPI + PostgreSQL；路由 app/api/，业务 app/services/，模型 app/models/；配置由 app/config/settings.py 管理，数据库使用 DATABASE_URL。
- miniprogram/ 为管理端，miniprogram-client/ 为客户端；appid、登录态、权限和发布包独立。只修改任务涉及的端。
- 英文代码命名，中文注释、文档和错误提示。后端按资源组织 API，使用 HTTPException 返回中文结构化错误。
- 权限、扣卡次数等业务约束由后端执行，不能只靠界面隐藏。检查本次入口、操作日志、共享接口及相关端联动，保持扣卡与交易数据一致，不遗漏已承诺的端。
- 优先现有组件、封装和状态管理；飞书任务复用现有服务并核对远端字段与模型，远端写入遵守上述授权边界。

## 按需规范与完成标准

- PC 界面：[wyyard-ui-design](.agents/skills/wyyard-ui-design/SKILL.md)；小程序开发/排查：[miniprogram-dev](.agents/skills/miniprogram-dev/SKILL.md)；修改后验证、测试或提交：[wyyard-verify](.agents/skills/wyyard-verify/SKILL.md)。
- 用户指定设计与参考页面优先，未指定时采用 [视觉基线](UI-DESIGN-GUIDELINES.md)，不为统一样式改变业务含义。
- 测试采用能覆盖本次风险的最小集合，优先定向检查、复用同一版本结果；只有高风险、影响不明或用户要求才扩大，不默认全量测试或全仓扫描。具体选择见验证 Skill。
- 范围内实现、联动及必要验证完成且无本次回归才算完成；关键验证受阻时明确说“已实现，尚有未验证项”。无关失败须有证据，不为全绿扩展任务；检查通过且无新风险后收尾。
- 最终简述结果、验证及真正未完成项，不将已提交、已推送、已部署混为一谈。

## Git

- 未要求不主动提交或推送。“提交”仅本地 commit；“上传 Git”包含提交并正常推送本次相关改动，不包含 force push 或部署。
- 操作前核对分支、remote 与差异；目标唯一明确就执行，否则先解决目标或改动范围问题。只暂存相关内容，排除密钥、依赖目录、缓存、构建产物及无关备份/预览。
- commit 用简洁中文；不无故切分支，新建分支遵从宿主命名规则（Codex 默认 codex/）。
