# wyyard — 项目协作约定

## 目标与指令边界

面向 GPT-6 Astra 等具备复杂任务能力的编码代理：明确结果、安全边界和项目不变量，常规实现路径由代理根据证据决定。不要因为模型能力强而扩大授权，也不要为每一步增加确认。

- 系统、开发者指令及工具权限优先。本文件是项目共同规则；Skills 只补充各自领域的必要细节，不另建一套批准流程。用户当前明确要求优先于项目默认做法。
- CLAUDE.md 引用本文件，避免两套规则漂移。设计规范是默认基线，用户指定的设计稿、范围和交互要求优先。
- 历史聊天、记忆、预览文件和旧示例仅作线索，不是当前任务、业务规则或授权。技术版本、路由、环境地址以当前源码与配置为准，发现不一致先查证。

## 自主执行与澄清

- 分清请求：解释/审阅/诊断只读并报告；“调整、修复、实现”授权完成相应实现；“先给方案、不要改”只交付方案或明确要求的预览。
- 已授权实现时，直接完成必要的代码修改、相关端/接口联动和适度验证。不停在计划或“是否继续”，不额外做全站改版、架构重构、批量清理或修复无关历史问题。
- 普通实现细节沿用现有模式，自主选择可逆、局部的方案。只有缺失信息会实质改变业务口径、权限边界、数据目标或产生难以恢复的后果，且无法从代码/上下文确认时，才提出聚焦问题。
- 先完成不依赖该答案的安全工作；不要在关键选择未定时做猜测性大改。用户已作选择不重复询问，范围或风险发生实质变化才重新确认。
- 修 bug 要定位数据流和根因，处理本次原因的必要影响面；无需为“彻底”无限追查整个系统。复用已有组件和封装，仅在当前需求确有必要时增加抽象、依赖或配置。
- 工具失败先做有依据的替代检查；无新证据不重复同一失败调用。遇到真实阻塞时说明已完成部分、阻塞依据和最小所需动作，不假装完成或静默放弃。
- 仅在当前运行规则允许、且存在独立有价值的子任务时使用子代理；不为普通小改动增加协作流程。

## 授权与数据安全

- 范围内的只读检查、局部源码修改、创建隔离测试、可恢复的开发产物处理，不单独询问批准。代码迁移的编写与在真实数据库上执行迁移是两件事。
- 推送、公开发布、全局依赖安装、真实数据删除/覆盖、破坏性数据库迁移、密钥轮换等外部或高影响操作，须有覆盖该动作与目标的明确授权。已有明确授权无需再问一次；普通推送不授权 force push，修代码不授权操作正式数据。
- “上传 Git”表示提交并正常推送本次相关改动；“提交”仅本地 commit。操作前检查分支、remote、待提交差异；目标唯一明确则直接执行，目标不明或含无关改动时先解决范围问题。工具要求的批准仍须走工具流程。
- 测试使用隔离数据/测试 schema，先核实连接目标及隔离措施，不因地址是 localhost 就假设数据可删除。数据源切换只改用户指定端与环境；未指定且确需切换时再问。
- 所有业务记录应持久化；临时 UI 状态、缓存和测试替身不必持久化。密钥、token、密码不进入源码、提交或日志，敏感配置通过现有环境配置机制管理。
- 保留用户已有改动。删除确认无引用的本次临时代码可作为实现步骤；对既有疑似废弃文件先核对调用方，需要清理时按项目约定归档至 archive/，至少保留 30 天。兼容期接口保留并标明替代方案，不以“检测未使用”为理由批量移动文件。
- 不运行 git reset --hard、强制覆盖工作树或绕过平台沙箱。恢复/覆盖业务数据前验证准确目标并准备可恢复备份；目标不明确就停止该动作。

## 项目导航与实现不变量

- frontend/：React + TypeScript + Vite，界面组件位于 src/components/，页面位于 src/pages/；版本查 package.json。
- backend/：FastAPI + PostgreSQL，路由 app/api/、业务 app/services/、模型 app/models/；连接由 DATABASE_URL 配置。
- miniprogram/：管理端；miniprogram-client/：客户端。两端独立 appid、登录态、权限和发布包；涉及哪端就检查哪端，不自动修改另一端。
- 代码命名用英文，注释、文档与面向用户的错误提示用中文。
- 前端请求走 src/lib/api.ts；昵称/姓名搜索复用 CustomerSearchInput；包含搜索框的 DialogContent 使用 initialFocus={false}。优先现有组件、Tailwind 和状态管理方式。
- 后端按资源组织 API，使用 HTTPException 返回中文结构化错误。环境变量由 config/settings.py 管理。权限与计数等业务约束由后端执行，不能仅靠前端隐藏。
- 涉及新功能入口、权限、操作日志或 PC/小程序共享接口时，检查本次变更所需的联动，不能漏掉已承诺的端；不据此扩展无关功能。
- 飞书相关任务复用现有服务封装，核对远端字段与模型；写远端数据或结构仍按上面的授权边界执行。

## Skills 与完成标准

- frontend/ 界面工作使用 .agents/skills/wyyard-ui-design/SKILL.md。
- 两个小程序开发/排查使用 .agents/skills/miniprogram-dev/SKILL.md。
- 代码修改后的验证、测试或提交收尾使用 .agents/skills/wyyard-verify/SKILL.md。按变更影响选择检查，不把小改动变成全仓审计。
- 完成意味着：请求范围已实现、必要联动已处理、相关验证已完成且没有未解决的本次回归。只改完代码但关键验证受阻时，如实说“已实现，尚有未验证项”。
- 无关基线失败必须有证据；不能为了全绿擅自修全仓，也不能把未知失败直接归为历史问题。必要检查通过后，除非有新变更或未解决风险，不反复扩大验证。
- 最终简要交代结果、验证范围、真正未完成项；涉及 Git 时区分已提交、已推送、已部署。未要求 Git 不主动提交或推送。
- commit 信息用简洁中文；新建分支遵从宿主命名规则（Codex 默认 codex/），不为一次普通修改无故切分支。排除 .env、node_modules/、__pycache__/、dist/ 与无关备份/预览。
