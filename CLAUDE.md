# wyyard — 代理入口

项目共同规则见 [AGENTS.md](AGENTS.md)，开始项目工作时读取并遵循；不在此复制技术清单、批准或验证规则。

## 设计咨询

- 用户只要方案/参考样式时，不修改业务源码；只生成所需预览。
- 中小预览直接完成，不默认启动子代理。已有上下文足够时只核对相关源码，避免重复全量读取。
- 预览检查布局和交互即可，不运行业务构建；需要视觉判断时可查看预览，不禁止截图复核。
- 用户已选定方案或明确要求实现时直接落地，不再要求定稿确认。生产源码验证按 wyyard-verify 的影响范围执行，小程序不运行无关的 PC 构建。
